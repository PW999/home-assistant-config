---
###############################################################################
#   @author         :   Phillip (P_W999)
#   @date           :   01/2022
#   @package        :   Temperature offset
#   @description    :   Fix temp difference between Aqara and Osram sensor
#   @url            :   https://github.com/PW999/home-assistant-config/
#   @template:      :   ./sensor/template/sensor_pantry_temperature.erb
###############################################################################

sensor:
  - platform: template
    sensors:
      pantry_temperature_corrected:
        friendly_name: Pantry temperature
        unit_of_measurement: °C
        device_class: temperature
        availability_template: "{{ not is_state('sensor.pantry_door_temperature', 'unavailable')  }}"
        value_template: "{{ (float(states('sensor.pantry_door_temperature')) - 2.5) | round(1) }}"
